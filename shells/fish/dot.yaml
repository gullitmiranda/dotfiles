linux|darwin:
  # Fish shell configuration
  links:
    # NOTE: We don't directly link the folders since we want to autoload them
    fish_plugins: ~/.config/fish/fish_plugins

  installs:
    depends:
      - tools
    cmd: |
      # Install Fish shell and Fisher
      brew install --quiet fish fisher

      export DOTFILES_DIR="{{ config.dotfiles }}"
      {{ config.dotfiles }}/shells/fish/install.fish
